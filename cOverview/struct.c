#include <stdio.h>
#include <stdlib.h>

// ENUM in Java
struct Student{
    char name[50];
    char major[50];
    int age;
    double gpa;
}

int main()
{
    struct Student student1;
    student1.age = 22;
    student.gpa = 3.2;
    strcpy( studen1.name, "Jim");
    strcpy( studen1.major, "Medicin");

    return 0;
}
